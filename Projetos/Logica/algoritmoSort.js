const sort = (input) => {
    let array = []

    // array = input.charAt(1)



    for(let i = 0; input[i] != null; i++) {
        array[i] = input.charAt(i)
    }

    console.log(array)
}

sort("asassas")